spring:
  application:
    name: agent

github:
  owner: ${GITHUB_OWNER:dummy-owner}
  repo: ${GITHUB_REPO:dummy-repo}

anthropic:
  api-key: ${ANTHROPIC_API_KEY:dummy-key}
  model: ${ANTHROPIC_MODEL:claude-opus-4-20250514}
  timeout-seconds: ${ANTHROPIC_TIMEOUT_SECONDS:60}

mcp:
  base-url: ${MCP_BASE_URL:http://localhost:3333}
  path: ${MCP_PATH:/mcp}